import { ChangeEvent, useEffect, useRef, useState } from 'react'
import {
  BarChart3, Camera, Check, ChevronRight, ClipboardCheck, Download, FileImage,
  Home, ImagePlus, LayoutTemplate, Plus, ScanLine, UserRound, X,
} from 'lucide-react'
import LiveScanner from './components/LiveScanner'
import { averageScore, downloadCSV, gradeAnswers, GradedStudent, questionRates, toCSV } from './lib/grading'
import { AnswerCardTemplate, createAnswers, drawAnswerCard, OPTION_LABELS, Option, Recognition, recognizeAnswerCard } from './lib/omr'

type Page = 'home' | 'templates' | 'scan' | 'analysis' | 'profile'
const STORAGE_KEY = 'answer-sheet-manager.templates'

function loadTemplates(): AnswerCardTemplate[] {
  try { return JSON.parse(localStorage.getItem(STORAGE_KEY) ?? '[]') } catch { return [] }
}

function Toast({ message }: { message: string | null }) { return message ? <div className="toast" role="status"><Check size={16} />{message}</div> : null }
function BottomNav({ page, onChange }: { page: Page; onChange: (page: Page) => void }) { const items: Array<[Page, string, typeof Home]> = [['home', '首页', Home], ['templates', '答题卡', LayoutTemplate], ['scan', '扫描', ScanLine], ['profile', '我的', UserRound]]; return <nav className="bottom-nav">{items.map(([id, label, Icon]) => <button key={id} aria-label={label} onClick={() => onChange(id)} className={page === id ? 'active' : ''}><span className={id === 'scan' ? 'scan-nav-icon' : ''}><Icon size={id === 'scan' ? 23 : 21} /></span><small>{label}</small></button>)}</nav> }

function EmptyState({ onCreate }: { onCreate: () => void }) { return <section className="empty-state"><LayoutTemplate size={37} /><h2>还没有答题卡</h2><p>先创建一张标准答题卡，设置答案后即可打印并开始扫描阅卷。</p><button onClick={onCreate}><Plus size={18} />新建答题卡</button></section> }
function HomePage({ templates, onPage, onCreate }: { templates: AnswerCardTemplate[]; onPage: (page: Page) => void; onCreate: () => void }) { return <><header className="app-header"><div className="logo"><span><ClipboardCheck size={20} /></span><b>Answer Sheet Manager</b></div></header><main className="page home-page"><div className="welcome"><div><p>答题卡阅卷</p><h1>从一张标准答题卡开始</h1></div></div>{templates.length ? <><section className="continue-card"><div className="continue-info"><div className="continue-kicker"><ScanLine size={14} />准备就绪</div><h2>{templates[0].name}</h2><p>{templates[0].subject} · {templates[0].questionCount} 道单选题</p><span>标准答案已设置，可直接扫描阅卷</span></div><button aria-label="开始扫描" onClick={() => onPage('scan')} className="round-arrow"><ChevronRight size={22} /></button></section><section className="quick-section"><div className="section-head"><h2>快捷操作</h2></div><div className="quick-actions"><button onClick={() => onPage('scan')}><span className="quick-icon camera"><Camera size={23} /></span><b>扫描答卷</b><small>实时识别</small></button><button onClick={onCreate}><span className="quick-icon template"><Plus size={24} /></span><b>新建答题卡</b><small>设置答案</small></button><button onClick={() => onPage('templates')}><span className="quick-icon report"><LayoutTemplate size={23} /></span><b>管理模板</b><small>{templates.length} 张答题卡</small></button></div></section></> : <EmptyState onCreate={onCreate} />}</main></> }

function CardPreview({ template }: { template: AnswerCardTemplate }) { const ref = useRef<HTMLCanvasElement>(null); useEffect(() => { if (ref.current) drawAnswerCard(ref.current, template) }, [template]); return <canvas ref={ref} className="card-preview" /> }
function TemplatesPage({ templates, onCreate, onSelect }: { templates: AnswerCardTemplate[]; onCreate: () => void; onSelect: (template: AnswerCardTemplate) => void }) { return <><header className="page-top"><div><h1>答题卡</h1><p>只识别本系统生成的标准答题卡</p></div><button onClick={onCreate} className="create-mini" aria-label="新建答题卡"><Plus size={20} /></button></header><main className="page templates-page">{templates.length === 0 ? <EmptyState onCreate={onCreate} /> : <div className="template-list">{templates.map(template => <button className="real-template-card" onClick={() => onSelect(template)} key={template.id}><CardPreview template={template} /><div><span>{template.subject}</span><h2>{template.name}</h2><p>{template.questionCount} 道单选题 · 每题 5 分</p><small>点击编辑、打印或开始扫描</small></div><ChevronRight size={19} /></button>)}</div>}</main></> }

function TemplateDetail({ template, onBack, onAnswers, onScan, notify }: { template: AnswerCardTemplate; onBack: () => void; onAnswers: () => void; onScan: () => void; notify: (text: string) => void }) { const ref = useRef<HTMLCanvasElement>(null); useEffect(() => { if (ref.current) drawAnswerCard(ref.current, template) }, [template]); const download = () => { if (!ref.current) return; const link = document.createElement('a'); link.href = ref.current.toDataURL('image/png'); link.download = `${template.name}-答题卡.png`; link.click(); notify('答题卡 PNG 已下载，可直接打印') }; return <><header className="page-top"><div><h1>{template.name}</h1><p>{template.subject} · {template.questionCount} 道题</p></div><button aria-label="返回答题卡列表" className="header-icon" onClick={onBack}><X size={21} /></button></header><main className="page detail-page"><div className="print-preview"><canvas ref={ref} /></div><section className="detail-actions"><button onClick={onAnswers}><Check size={19} />设置标准答案</button><button onClick={download}><Download size={19} />下载打印答题卡</button><button className="primary-action" onClick={onScan}><Camera size={19} />开始扫描阅卷</button></section><p className="real-note">答题卡四角的黑色定位方块必须完整打印且拍摄时清晰可见，识别器会据此校正纸张角度。</p></main></> }

function AnswerEditor({ template, onSave, onClose }: { template: AnswerCardTemplate; onSave: (answers: Option[]) => void; onClose: () => void }) { const [answers, setAnswers] = useState(template.answers); return <div className="modal-backdrop"><section className="template-modal answer-modal"><header><button onClick={onClose} aria-label="关闭答案设置"><X size={21} /></button><h2>设置标准答案</h2><button className="modal-save" onClick={() => onSave(answers)}>保存</button></header><p className="answer-modal-tip">每题仅支持一个正确选项。答案会作为实时对错浮层和最终判分依据。</p><div className="answer-editor">{answers.map((answer, index) => <div className="answer-row" key={index}><span>第 {index + 1} 题</span>{OPTION_LABELS.map(option => <button className={answer === option ? 'on' : ''} onClick={() => setAnswers(current => current.map((value, item) => item === index ? option : value))} key={option}>{option}</button>)}</div>)}</div><button className="create-template-button" onClick={() => onSave(answers)}><Check size={19} />保存标准答案</button></section></div> }

function NewTemplate({ onCreate, onClose }: { onCreate: (template: AnswerCardTemplate) => void; onClose: () => void }) { const [name, setName] = useState(''); const [subject, setSubject] = useState('数学'); const [count, setCount] = useState(20); const create = () => { const cleanName = name.trim(); if (!cleanName) return; onCreate({ id: crypto.randomUUID(), name: cleanName, subject, questionCount: count, answers: createAnswers(count), createdAt: new Date().toISOString() }) }; return <div className="modal-backdrop"><section className="template-modal"><header><button onClick={onClose} aria-label="关闭新建答题卡"><X size={21} /></button><h2>新建答题卡</h2><span></span></header><div className="form-block"><label>答题卡名称<input value={name} onChange={event => setName(event.target.value)} placeholder="例如：第一单元测验" autoFocus /></label><label>试卷科目<select value={subject} onChange={event => setSubject(event.target.value)}><option>数学</option><option>语文</option><option>英语</option><option>物理</option><option>化学</option></select></label><label>单选题数量<div className="count-control"><button aria-label="减少题目数量" onClick={() => setCount(value => Math.max(1, value - 1))}>−</button><b>{count} 题</b><button aria-label="增加题目数量" onClick={() => setCount(value => Math.min(60, value + 1))}>+</button></div></label></div><button className="create-template-button" disabled={!name.trim()} onClick={create}><Plus size={19} />创建标准答题卡</button></section></div> }

function ScanPage({ template, onSelect, onScanned, notify }: { template: AnswerCardTemplate | null; onSelect: () => void; onScanned: (recognition: Recognition, fileName: string) => void; notify: (text: string) => void }) { const fileRef = useRef<HTMLInputElement>(null); const [processing, setProcessing] = useState(false); const [scanner, setScanner] = useState(false); const importImage = async (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (!file || !template) return; setProcessing(true); try { const url = URL.createObjectURL(file); const image = new Image(); image.onload = () => { try { onScanned(recognizeAnswerCard(image, image.naturalWidth, image.naturalHeight, template), file.name) } finally { URL.revokeObjectURL(url); setProcessing(false) } }; image.onerror = () => { URL.revokeObjectURL(url); setProcessing(false); notify('图片无法读取') }; image.src = url } catch { setProcessing(false); notify('图片识别失败') } }; if (scanner && template) return <LiveScanner template={template} onClose={() => setScanner(false)} onConfirm={recognition => { setScanner(false); onScanned(recognition, `camera-${Date.now()}.jpg`) }} />; return <><header className="page-top scan-top"><div><h1>扫描答卷</h1><p>{template ? `${template.name} · ${template.questionCount} 道题` : '请先选择答题卡模板'}</p></div></header><main className="page scan-page">{template ? <><button onClick={onSelect} className="exam-picker"><div className="exam-icon"><LayoutTemplate size={21} /></div><div><small>当前答题卡</small><b>{template.name}</b><span>{template.subject} · {template.questionCount} 道单选题</span></div><ChevronRight size={19} /></button><section className="scan-zone"><div className="scan-frame"><span></span><span></span><span></span><span></span><Camera size={38} /></div><h2>实时扫描答题卡</h2><p>将系统生成答题卡的四个定位方块完整放入画面，系统会实时显示对错浮层。</p><button onClick={() => setScanner(true)} className="scan-button"><Camera size={20} />打开实时相机</button><button onClick={() => fileRef.current?.click()} className="album-button"><ImagePlus size={19} />导入已拍摄答题卡</button><input ref={fileRef} onChange={importImage} type="file" accept="image/*" hidden /></section>{processing && <p className="scan-tip"><ScanLine size={15} />正在读取答题卡真实填涂结果</p>}<p className="real-note">导入图片只适用于正面、完整、未裁切的答题卡。倾斜拍摄请使用实时相机模式。</p></> : <EmptyState onCreate={onSelect} />}</main></> }

function ReviewPage({ template, recognition, fileName, onSave, onCancel }: { template: AnswerCardTemplate; recognition: Recognition; fileName: string; onSave: (answers: Array<Option | null>, confidence: number[]) => void; onCancel: () => void }) { const [answers, setAnswers] = useState(recognition.answers); const unresolved = answers.filter(answer => answer === null).length; return <><header className="page-top"><div><h1>确认识别结果</h1><p>{recognition.markerValid ? '定位标记校验通过' : '定位标记不完整，请人工确认'}</p></div><button className="header-icon" onClick={onCancel} aria-label="取消本次识别"><X size={21} /></button></header><main className="page review-page"><div className={recognition.markerValid ? 'review-status pass' : 'review-status warning'}><Check size={19} /><span>{unresolved ? `${unresolved} 题未识别，请手动选择` : '全部题目已识别，可确认批改'}</span></div><section className="review-grid">{answers.map((answer, index) => <div className={answer === template.answers[index] ? 'review-question correct' : 'review-question wrong'} key={index}><div><b>第 {index + 1} 题</b><small>正确：{template.answers[index]}</small></div><div>{OPTION_LABELS.map(option => <button key={option} onClick={() => setAnswers(current => current.map((value, item) => item === index ? option : value))} className={answer === option ? 'selected' : ''}>{option}</button>)}</div></div>)}</section><button onClick={() => onSave(answers, recognition.confidence)} className="create-template-button"><Check size={19} />确认批改并保存</button><small className="file-name"><FileImage size={14} />{fileName}</small></main></> }

function AnalysisPage({ template, records, onBack }: { template: AnswerCardTemplate | null; records: GradedStudent[]; onBack: () => void }) { if (!template || !records.length) return <><header className="page-top"><div><h1>成绩分析</h1><p>真实识别记录将在这里展示</p></div></header><main className="page analysis-page"><section className="analysis-empty"><BarChart3 size={34} /><h2>暂无阅卷记录</h2><p>创建标准答题卡后，通过相机或导入图片完成识别和人工复核。</p></section></main></>; const rates = questionRates(records, template.questionCount); const average = averageScore(records); return <><header className="page-top"><div><h1>成绩分析</h1><p>{template.name} · 已阅 {records.length} 份</p></div><button onClick={onBack} className="header-icon" aria-label="返回首页"><Home size={20} /></button></header><main className="page analysis-page"><section className="score-hero"><span>班级平均分</span><b>{average.toFixed(1)}</b><small>/ {template.questionCount * 5} 分</small><p>{records.length} 份真实识别答卷</p></section><section className="analysis-grid"><div><b>{Math.max(...records.map(record => record.score))}</b><span>最高分</span></div><div><b>{Math.min(...records.map(record => record.score))}</b><span>最低分</span></div><div><b>{Math.round(rates.reduce((sum, rate) => sum + rate, 0) / rates.length)}%</b><span>平均正确率</span></div></section><section className="analysis-section"><div className="section-head"><h2>题目正确率</h2><span className="section-sub">真实识别</span></div>{rates.map((rate, index) => <div className="rate-row" key={index}><span>第 {index + 1} 题</span><div><i className={rate < 60 ? 'low' : ''} style={{ width: `${rate}%` }}></i></div><b>{rate}%</b></div>)}</section><section className="analysis-section"><div className="section-head"><h2>成绩明细</h2><button onClick={() => downloadCSV(`${template.name}-成绩表.csv`, toCSV(template, records))}><Download size={13} />导出 CSV</button></div><div className="students-table"><div className="students-header"><span>姓名</span><span>答对题数</span><span>得分</span></div>{records.map(record => <div className="student-row" key={record.fileName}><span>{record.name}</span><span>{record.correctCount} / {template.questionCount}</span><b>{record.score}</b></div>)}</div></section></main></> }

function ProfilePage({ templateCount, onPage }: { templateCount: number; onPage: (page: Page) => void }) { return <><header className="page-top"><div><h1>我的</h1><p>所有数据仅保存在当前浏览器</p></div></header><main className="page profile-page"><section className="profile-card"><span className="profile-avatar">教</span><div><h2>本地阅卷工作台</h2><p>未连接云端账户</p></div></section><section className="profile-stats"><button onClick={() => onPage('templates')}><b>{templateCount}</b><span>答题卡</span></button><button onClick={() => onPage('analysis')}><b>本机</b><span>成绩数据</span></button><button><b>H5</b><span>识别模式</span></button></section></main></> }

export default function App() { const [page, setPage] = useState<Page>('home'); const [templates, setTemplates] = useState(loadTemplates); const [selected, setSelected] = useState<AnswerCardTemplate | null>(null); const [createOpen, setCreateOpen] = useState(false); const [answerOpen, setAnswerOpen] = useState(false); const [detailOpen, setDetailOpen] = useState(false); const [review, setReview] = useState<{ recognition: Recognition; fileName: string } | null>(null); const [records, setRecords] = useState<Record<string, GradedStudent[]>>({}); const [message, setMessage] = useState<string | null>(null); useEffect(() => { localStorage.setItem(STORAGE_KEY, JSON.stringify(templates)) }, [templates]); const notify = (text: string) => { setMessage(text); window.setTimeout(() => setMessage(null), 2200) }; const create = (template: AnswerCardTemplate) => { setTemplates(current => [template, ...current]); setSelected(template); setCreateOpen(false); setDetailOpen(true); notify('标准答题卡已创建，请先设置答案并下载打印') }; const select = (template: AnswerCardTemplate) => { setSelected(template); setDetailOpen(true) }; const saveAnswers = (answers: Option[]) => { if (!selected) return; const updated = { ...selected, answers }; setTemplates(current => current.map(template => template.id === updated.id ? updated : template)); setSelected(updated); setAnswerOpen(false); notify('标准答案已保存') }; const scanned = (recognition: Recognition, fileName: string) => { if (!recognition.markerValid) { notify('未检测到完整定位标记，请使用实时相机重拍'); return }; setReview({ recognition, fileName }) }; const saveReview = (answers: Array<Option | null>, confidence: number[]) => { if (!selected || !review) return; const record = gradeAnswers(selected, `答卷 ${new Date().toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' })}`, review.fileName, answers, confidence); setRecords(current => ({ ...current, [selected.id]: [...(current[selected.id] ?? []), record] })); setReview(null); setPage('analysis'); notify('真实识别结果已保存') }; return <div className="mobile-app">{page === 'home' && <HomePage templates={templates} onPage={setPage} onCreate={() => setCreateOpen(true)} />}{page === 'templates' && <TemplatesPage templates={templates} onCreate={() => setCreateOpen(true)} onSelect={select} />}{page === 'scan' && <ScanPage template={selected ?? templates[0] ?? null} onSelect={() => setPage('templates')} onScanned={scanned} notify={notify} />}{page === 'analysis' && <AnalysisPage template={selected} records={selected ? records[selected.id] ?? [] : []} onBack={() => setPage('home')} />}{page === 'profile' && <ProfilePage templateCount={templates.length} onPage={setPage} />}{detailOpen && selected && <TemplateDetail template={selected} onBack={() => setDetailOpen(false)} onAnswers={() => setAnswerOpen(true)} onScan={() => { setDetailOpen(false); setPage('scan') }} notify={notify} />}{createOpen && <NewTemplate onCreate={create} onClose={() => setCreateOpen(false)} />}{answerOpen && selected && <AnswerEditor template={selected} onSave={saveAnswers} onClose={() => setAnswerOpen(false)} />}{review && selected && <ReviewPage template={selected} recognition={review.recognition} fileName={review.fileName} onSave={saveReview} onCancel={() => setReview(null)} />}<Toast message={message} /><BottomNav page={page} onChange={setPage} /></div> }
